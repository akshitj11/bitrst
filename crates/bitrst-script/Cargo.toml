[package]
name = "bitrst-script"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
bitrst-crypto = { path = "../bitrst-crypto" }
thiserror = "1"

[dev-dependencies]
hex = "0.4"
secp256k1 = { version = "0.29", features = ["global-context", "rand"] }
