[package]
name = "bitrst-wallet"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
bitrst-core = { path = "../bitrst-core" }
bitrst-crypto = { path = "../bitrst-crypto" }
bitrst-script = { path = "../bitrst-script" }
secp256k1 = { version = "0.29", features = ["global-context", "rand-std"] }
thiserror = "1"

[dev-dependencies]
hex = "0.4"
