[package]
name = "bitrst-core"
version = "0.1.0"
edition = "2021"

[dependencies]
bitrst-crypto = { path = "../bitrst-crypto" }

[dev-dependencies]
hex = "0.4"
bitrst-crypto = { path = "../bitrst-crypto" }
