[package]
name = "bitrst-core"
version = "0.1.0"
edition = "2021"

[dependencies]
bitrst-crypto = { path = "../bitrst-crypto" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[features]
test-short-period = []

[dev-dependencies]
hex = "0.4"
serde_json = "1"
